Current status: SDK test (src/test_sdk.py) runs and streams messages, but returns Not logged in · Please run /login. Authentication via Claude Code login or API key is still to be configured.



git add .
git commit -m "Wire Claude Agent SDK test and project structure"
git push